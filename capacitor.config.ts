import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.hardwarebuddy.com',
  appName: 'Hardware Buddy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
